using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class ItemsEditor : Editor
{
    //public override void OnGUI(Rect position, SerializedProperty property, GUIContent label)
    //{
    //    base.OnGUI(position, property, label);

    //    EditorGUI.BeginProperty(position, label, property);

    //    position = EditorGUI.PrefixLabel(position, GUIUtility.GetControlID(FocusType.Passive), label);
    //    position.height = 16;



    //    SerializedProperty arrayProp = property.FindPropertyRelative("")

    //    var obj = fieldInfo.GetValue(property.serializedObject.targetObject);
    //    Items iI = obj as Items;

    //    if (obj.GetType().IsArray)
    //    {
    //        var index = System.Convert.ToInt32(new string(property.propertyPath.ToCharArray()));
    //        SerializedProperty s = property.GetArrayElementAtIndex(index);

    //        EditorGUI.PropertyField(position, s);
    //    }

    //    if (iI is Melee m)
    //    {

    //    }
    //    else if (iI is Range r)
    //    {

    //    }

    //    EditorGUI.EndProperty();


    //}
}